# The data pipeline needs nothing — Python 3.8+ stdlib only.
# That's deliberate: no dependency can break your nightly ingest.

# The web app needs these:
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Tests (TestClient):
httpx>=0.27

# Optional: only needed for `prices --provider yfinance`. The pipeline's
# stooq/eodhd paths still need nothing but stdlib -- yfinance is a real
# dependency tree (pandas, numpy, lxml, curl_cffi...), so it's opt-in,
# lazily imported inside screener/prices.py rather than imported at module
# load. pip install yfinance if you want this provider.
# yfinance>=1.7
