# The data pipeline needs nothing — Python 3.8+ stdlib only.
# That's deliberate: no dependency can break your nightly ingest.

# The web app needs these:
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Tests (TestClient):
httpx>=0.27
